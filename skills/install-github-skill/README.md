# install-github-skill

Use `/skill:install-github-skill <github-url>` to install an Agent Skill from GitHub. The installer prompts for target (Pi, Claude Code, Codex, .agents, or custom path). Use `--yes` for non-interactive default (`pi-project`) when no target/path is given.
